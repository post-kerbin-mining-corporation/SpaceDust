// Defines Antimatter definitions for stock planets

// KERBIN
// =============
SPACEDUST_RESOURCE
{
  resourceName = Antimatter
  body = Kerbin
  RESOURCEBAND
  {
    name = kerbRing
    title = #LOC_SpaceDust_Band_LowBelt

    // Discoverability Data
    // --------------
    alwaysDiscovered = true
    alwaysIdentified = false

    // Maximum and minimum abundances (variation is by game seed)
    // In t/m^3
    minAbundance = 0.00000041
    maxAbundance = 0.00000121

    // Scale abundances by air density
    useAirDensity = False
    // Distribution model to use, can be Uniform or Spherical
    distributionType = Spherical

    // These parameters are specific to the Spherical model
    // -------
    // Altitudes are in km from sea level
    // Maximum altitude for a distribution
    altUpperBound = 650000
    // minimum altitude for a distribution
    altLowerBound = 550000
    // peak of a distribution
    altPeak = 600000
    // maximum variation of these altitudes (variation is by game seed)
    altVariability = 10
    // Falloff for altitude, can be Linear or None
    altFalloffType = Linear
    altitudeSquish = 0.9
    // lats in degrees
    // Maximum latitude for a distribution
    latUpperBound = 40
    // Minimum latitude for a distribution
    latLowerBound = -40
    // peak of a distribution
    latPeak = 0
    // maximum variation of these latitudes (variation is by game seed)
    latVariability = 1
    // Falloff for latitude, can be Linear or None
    latFalloffType = Linear
  }

  RESOURCEBAND
  {
    name = kerbRingHigher
    title = #LOC_SpaceDust_Band_HighBelt

    // Discoverability Data
    // --------------
    alwaysDiscovered = false
    alwaysIdentified = false

    // Maximum and minimum abundances (variation is by game seed)
    // In t/m^3
    minAbundance = 0.00000410
    maxAbundance = 0.00001210

    // Scale abundances by air density
    useAirDensity = False
    // Distribution model to use, can be Uniform or Spherical
    distributionType = Spherical

    // These parameters are specific to the Spherical model
    // -------
    // Altitudes are in km from sea level
    // Maximum altitude for a distribution
    altUpperBound = 2100000
    // minimum altitude for a distribution
    altLowerBound = 1900000
    // peak of a distribution
    altPeak = 2000000
    // maximum variation of these altitudes (variation is by game seed)
    altVariability = 10
    // Falloff for altitude, can be Linear or None
    altFalloffType = Linear
    altitudeSquish = 1

    // lats in degrees
    // Maximum latitude for a distribution
    latUpperBound = 55
    // Minimum latitude for a distribution
    latLowerBound = -55
    // peak of a distribution
    latPeak = 0
    // maximum variation of these latitudes (variation is by game seed)
    latVariability = 1
    // Falloff for latitude, can be Linear or None
    latFalloffType = Linear
  }

}


// JOOL
// =============

SPACEDUST_RESOURCE
{
  resourceName = Antimatter
  body = Jool

  RESOURCEBAND
  {
    name = joolExo
    title = #LOC_SpaceDust_Band_LowBelt

    // Maximum and minimum abundances (variation is by game seed)
    // In t/m^3
    minAbundance = 0.00000041
    maxAbundance = 0.00000121

    // Visuals Data
    // -------------
    countScale = 2
    rotateRate = 1
    
    // Discoverability Data
    // --------------
    alwaysDiscovered = false
    alwaysIdentified = false

    // Scale abundances by air density
    useAirDensity = False
    // Distribution model to use, can be Uniform or Spherical
    distributionType = Spherical

    // These parameters are specific to the Spherical model
    // -------
    // Altitudes are in km from sea level
    // Maximum altitude for a distribution
    altUpperBound = 600000
    // minimum altitude for a distribution
    altLowerBound = 400000
    // peak of a distribution
    altPeak = 500000
    // maximum variation of these altitudes (variation is by game seed)
    altVariability = 0
    // Falloff for altitude, can be Linear or None
    altFalloffType = Linear

    // lats in degrees
    // Maximum latitude for a distribution
    latUpperBound = 25
    // Minimum latitude for a distribution
    latLowerBound = -25
    // peak of a distribution
    latPeak = 0
    // maximum variation of these latitudes (variation is by game seed)
    latVariability = 6
    // Falloff for latitude, can be Linear or None
    latFalloffType = None
  }

  RESOURCEBAND
  {
    name = laytheAreaRing

    title = #LOC_SpaceDust_Band_Ring

    // Maximum and minimum abundances (variation is by game seed)
    // In t/m^3
    minAbundance = 0.0000041
    maxAbundance = 0.0000073

     // Visuals Data
    // -------------
    countScale = 20
    rotateRate = 0.25

    // Discoverability Data
    // --------------
    alwaysDiscovered = false
    alwaysIdentified = false

    // Scale abundances by air density
    useAirDensity = False
    // Distribution model to use, can be Uniform or Spherical
    distributionType = Spherical

    // These parameters are specific to the Spherical model
    // -------
    // Altitudes are in km from sea level
    // Maximum altitude for a distribution
    altUpperBound = 23000000
    // minimum altitude for a distribution
    altLowerBound = 21000000
    // peak of a distribution
    altPeak = 22000000
    // maximum variation of these altitudes (variation is by game seed)
    altVariability = 0
    // Falloff for altitude, can be Linear or None
    altFalloffType = Linear

    // lats in degrees
    // Maximum latitude for a distribution
    latUpperBound = 2
    // Minimum latitude for a distribution
    latLowerBound = -2
    // peak of a distribution
    latPeak = 0
    // maximum variation of these latitudes (variation is by game seed)
    latVariability = 0
    // Falloff for latitude, can be Linear or None
    latFalloffType = None
  }
}

// LAYTHE
// =========

SPACEDUST_RESOURCE
{
  resourceName = Antimatter
  body = Laythe
  RESOURCEBAND
  {
    name = laytheRing
    title = #LOC_SpaceDust_Band_LowBelt

    // Discoverability Data
    // --------------
    alwaysDiscovered = false
    alwaysIdentified = false

    // Maximum and minimum abundances (variation is by game seed)
    // In t/m^3
    minAbundance = 0.0000071
    maxAbundance = 0.00001121

    // Scale abundances by air density
    useAirDensity = False
    // Distribution model to use, can be Uniform or Spherical
    distributionType = Spherical

    // These parameters are specific to the Spherical model
    // -------
    // Altitudes are in km from sea level
    // Maximum altitude for a distribution
    altUpperBound = 2550000
    // minimum altitude for a distribution
    altLowerBound = 500000
    // peak of a distribution
    altPeak = 600000
    // maximum variation of these altitudes (variation is by game seed)
    altVariability = 10
    // Falloff for altitude, can be Linear or None
    altFalloffType = Linear
    altitudeSquish = 0.9
    // lats in degrees
    // Maximum latitude for a distribution
    latUpperBound = 40
    // Minimum latitude for a distribution
    latLowerBound = -40
    // peak of a distribution
    latPeak = 0
    // maximum variation of these latitudes (variation is by game seed)
    latVariability = 1
    // Falloff for latitude, can be Linear or None
    latFalloffType = Linear
  }


}